<Window x:Class="EtherNetIPTool.Views.HelpWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:EtherNetIPTool.ViewModels"
        mc:Ignorable="d"
        Title="{Binding Title}"
        Height="700"
        Width="950"
        WindowStartupLocation="CenterOwner"
        Background="#F0F0F0"
        FontFamily="Segoe UI"
        FontSize="9pt"
        ShowInTaskbar="False">

    <DockPanel>
        <!-- Button Panel -->
        <Border DockPanel.Dock="Bottom" Background="#E8E8E8" BorderBrush="#D0D0D0" BorderThickness="0,1,0,0" Padding="10">
            <Button Content="Close"
                    Command="{Binding CloseCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                    Width="100"
                    Height="24"
                    HorizontalAlignment="Right"
                    IsDefault="True"
                    ToolTip="Close help window"/>
        </Border>

        <!-- WebBrowser for HTML content -->
        <Border BorderBrush="#D0D0D0" BorderThickness="1" Margin="0" Background="White">
            <WebBrowser x:Name="HelpBrowser" Margin="0"/>
        </Border>
    </DockPanel>
</Window>
