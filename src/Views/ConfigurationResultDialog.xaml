<Window x:Class="EtherNetIPTool.Views.ConfigurationResultDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Configuration Result"
        Height="350"
        Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F0F0F0"
        FontFamily="Segoe UI"
        FontSize="9pt"
        ShowInTaskbar="False">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Icon and Title -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center">
            <TextBlock x:Name="IconText"
                       Text="✓"
                       FontSize="24pt"
                       FontWeight="Bold"
                       Foreground="Green"
                       Margin="0,0,10,0"
                       VerticalAlignment="Center"/>
            <TextBlock x:Name="TitleText"
                       Text="Configuration Successful"
                       FontSize="12pt"
                       FontWeight="Bold"
                       VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Summary -->
        <TextBlock Grid.Row="2"
                   x:Name="SummaryText"
                   Text=""
                   HorizontalAlignment="Center"
                   TextWrapping="Wrap"/>

        <!-- Detailed Results (REQ-3.5.5-008) -->
        <Border Grid.Row="4"
                BorderBrush="#D0D0D0"
                BorderThickness="1"
                Background="White"
                Padding="10">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="DetailsText"
                           Text=""
                           FontFamily="Consolas"
                           FontSize="8pt"
                           TextWrapping="Wrap"/>
            </ScrollViewer>
        </Border>

        <!-- OK Button -->
        <Button Grid.Row="6"
                Content="OK"
                Width="80"
                Height="28"
                HorizontalAlignment="Center"
                IsDefault="True"
                Click="OkButton_Click"/>
    </Grid>
</Window>
